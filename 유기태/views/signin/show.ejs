<!DOCTYPE html>
<html lang="ko">
    <head>
        <meta charset="UTF-8">
        <title>Pentago</title>
        <link rel="stylesheet" href="/stylesheets/style.css">  
    </head>
    <body>
        <div class="hero">
            <div class="navbar">
                <div>
                    <h5><%= post.Title %></h5>
                    <div> <!-- 1 -->
            
                      <div> <!-- 1 -->
                        <div><%= post.Body %></div>
                      </div>
                    </div>
                </div>
                <form action="/ingame/<%= post._id %>?_method=delete" method="post">
                    <a href="#" onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">Delete</a>
                </form>
            </div>
        </div>
    </body>
</html>
